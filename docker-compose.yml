services:
  wsgi:
    build: .
    container_name: berea-wsgi
    image: berea-wsgi
    expose:
      - 8000
  
  nginx:
    build: ./nginx
    container_name: berea-nginx
    image: berea-nginx
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - wsgi
